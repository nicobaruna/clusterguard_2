{
  "name": "@clusterguard/api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "build": "tsc -p tsconfig.json",
    "test": "node --test --experimental-strip-types src/sos.test.ts",
    "deploy": "wrangler deploy --minify src/index.ts"
  },
  "dependencies": {
    "hono": "^4.6.11",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "@types/node": "^22.7.4",
    "typescript": "^5.5.4",
    "wrangler": "^3.4.0"
  }
}
