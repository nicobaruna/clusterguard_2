{
  "name": "clusterguard",
  "private": true,
  "workspaces": ["apps/web", "apps/api"],
  "scripts": {
    "dev": "npm run dev --workspace @clusterguard/web",
    "build": "npm run build --workspace @clusterguard/web",
    "typecheck": "npm run typecheck --workspace @clusterguard/web && npm run typecheck --workspace @clusterguard/api",
    "lint": "npm run lint --workspace @clusterguard/web",
    "test": "npm run test --workspace @clusterguard/api && npm run test --workspace @clusterguard/web",
    "db:migrate": "npx supabase db push --linked --yes"
  }
}
