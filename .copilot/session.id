ClusterGuard resume checkpoint

Updated: 2026-09-15 (FCM broadcast quality-gated)
Current phase: Phase 3 - Backend SOS API
Status: FCM broadcast quality-gated; commit/push approval required

Completed this session:
- Read all four project Markdown documents through a read-only scan.
- Consolidated the phased implementation plan into `.copilot/tasks.md`.
- User explicitly approved a destructive reset of both local rebuild artifacts and the linked Supabase database.
- Linked Supabase database reset completed successfully.
- Removed rebuildable local artifacts: `apps`, `supabase`, `package.json`, `package-lock.json`, `wrangler.toml`, and rebuilt `README.md`.
- Preserved `.env.local`, `.git`, all design Markdown files, `.copilot/tasks.md`, and `.copilot/session.id`.
- Recreated the npm workspace, web/API packages, clean baseline migration, and minimal app/test entrypoints.
- `npm install`, `npm run typecheck`, `npm run build`, and `npm test` pass.
- Re-linked Supabase and applied the clean baseline migration successfully.
- Verified `public.users`, `public.sos_events`, and `public.user_devices` exist with RLS enabled.
- Added `20260914000200_rls_hardening.sql` with controlled role helper, role-safe profile updates, owner-scoped devices, explicit SOS write revocation, and ownership indexes.
- Added migration contract tests and JWT sender-isolation coverage.
- Quality gates pass: typecheck, build, lint, tests (8/8), security review, and performance review.
- Applied RLS hardening migration to the linked Supabase project and verified RLS remains enabled on all three tables.
- `issue.log` contains the initial lint/security/performance loop findings; all actionable findings are resolved.
- Updated `.copilot/workflow.md`: after all gates and required migration verification pass, commit and push to the active branch are mandatory; force-push requires separate approval.
- `git diff --check` passed for the workflow update; branch is `dev` and a Git remote is configured.
- User explicitly authorized commit and push for this task.
- Final local gates pass after the ES2017 target fix: typecheck, lint, tests (8/8), and workspace-scoped web build exit 0.
- Committed `5ebcc5f feat(security): harden database row-level security` and pushed successfully to `dev`.
- Removed generated artifacts in cleanup commit `5b21774b270c` and pushed successfully to `dev`.
- Added rollback-only live RLS test at `supabase/tests/rls_adversarial.sql`.
- Live linked-database validation passed all 8 scenarios: anonymous isolation, WARGA profile/event/device isolation, role immutability, direct SOS-write denial, PIC visibility, and device-owner access. Transaction rolled back.
- Resolved the workspace build exit failure by regenerating the root lockfile with `npm install --package-lock-only`; workspace build now exits 0 with a non-fatal Next ENOWORKSPACES warning.
- Concurrent resolution remains deferred because no SOS resolve endpoint/function exists yet.
- User approved commit/push for the live RLS task.
- Commit `409a635f7b8b42c9eeb94f85e94347d88030de83 test(security): validate live row-level policies` was pushed to `dev`.
- Post-push verification: working tree clean; local HEAD equals origin/dev.
- Implemented Supabase Auth phone signup for WARGA/PIC and email login for SUPER_ADMIN.
- Added trusted profile/role loading from `public.users`, role mismatch rejection, session recovery via `getSession`, invalid-profile sign-out, and failure-safe form submission.
- Added Auth policy and contract tests; total test suite passes 13/13.
- Auth task quality gates pass: typecheck, lint, workspace web build, security review, and performance review.
- Migration step: not applicable; existing schema/trigger already supports phone profile creation and no database shape changed.
- Temporary Super Admin Auth test passed with a unique in-memory credential: Auth user creation, profile role update, anon email/password login, trusted profile role read, and cleanup all passed.
- No test user remains; no credential or password was persisted or reported.
- Auth regression suite after the provider test: 4 test files and 13 tests passed.
- Added browser-guarded local queue with sensitive payload rejection, storage failure reporting, retention pruning, and bounded queue capacity.
- Added sync manager with `online` event, interval retries, 401 handling, 4xx/429/5xx classification, exponential backoff, Web Locks, renewed lease fallback, and explicit stop cleanup.
- Started the sync manager from the web page lifecycle with bearer auth and `Idempotency-Key` transport.
- Added and applied `20260915000100_add_sos_idempotency.sql` with nullable UUID client key and partial unique index.
- Offline sync validation: API/web tests 21 total pass, typecheck/lint/build pass, security review pass, performance review pass.
- User approved commit/push for the offline-first task.
- Commit `2197943a23129dc90ad25dfa19cfee6e2f27755a feat(sync): add offline queue and background synchronization` pushed successfully to `dev`.
- Implemented Hono `POST /sos` with strict Zod validation, exact JWT protection, UUID sender validation, server-side Supabase REST persistence, and idempotency conflict recovery.
- Connected the web SOS client and offline queue to `/sos`, preserving the idempotency key across retries.
- SOS task tests, typecheck, lint, build, security, and performance gates pass.
- User approved commit/push for the concrete SOS task.
- Commit `4757e57d82a43d5b6cac1c98c26697d247b5bde3 feat(sos): persist authenticated events with idempotency` pushed successfully to `dev`.
- Implemented server-side FCM token lookup filtered to PIC/on-duty/mobile devices and asynchronous multicast broadcast through `waitUntil()`.
- FCM failures are caught and isolated from the successful persisted SOS response; idempotent retries do not rebroadcast.
- FCM task quality gates pass: tests (30/30), typecheck, lint, build, security review, and performance review.

Blocker:
- Live authenticated SOS persistence test remains pending; no permanent test credential was created.
- SOS resolve endpoint and concurrent resolution remain deferred.

Next action when resuming:
1. Obtain approval to commit/push the FCM broadcast task.
2. Implement SOS resolve endpoint and concurrent resolution behavior.
3. Add live authenticated SOS persistence verification.

Security notes:
- Do not read, print, commit, or copy values from `.env.local` into task or session files.
- Do not store plaintext passwords in `public.users`; Supabase Auth must own password verification.
